# DSE4101-CausalForest-Grp2
DSE4101 Capstone Project, Digital Economy, Causal Forest, Group 2

## Setup
After cloning, run:
    pip install -r requirements.txt
    python -m scripts.download_data
This downloads `purchases.csv.gz` (large file, not tracked in git) into `data/raw/x5/`.
